import 'package:flutter/material.dart';

class InitHomeScreen extends StatelessWidget {
  const InitHomeScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Screen de inicio"));
  }
}
